defaults:
  - _self_


# Mid-size circuit topology. Task lives in ``cfg.tasks``.

input_bits: 10
output_bits: 10
arity: 4
num_layers: 3
width_factor: 2
layer_sizes: null  # Auto-generate if null

circuit_hidden_dim: 32

init_logits: soft_wires
random_init_scale: 1.0
