defaults:
  - _self_


# Large circuit topology. Task lives in ``cfg.tasks``.

input_bits: 14
output_bits: 14
arity: 4
num_layers: 3
width_factor: 2
layer_sizes: null  # Auto-generate if null

circuit_hidden_dim: 64

init_logits: soft_wires
random_init_scale: 1.0
