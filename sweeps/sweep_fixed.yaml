# Define parameter sweeps
defaults:
  - override hydra/launcher: basic


sweep:
  grid:  # parameters to combine in all possible ways
    training.wiring_mode: [fixed, random]

    seed: [0, 1, 2, 4, 5]
    circuit: [reverse_large, add_large, binary_multiply_large]

    # FIXED PARAMETERS
    # model: [self_attention]
    model: [gathered_attention]
    circuit.circuit_hidden_dim: [64]
    model.attention_dim: [128]
    pool.noise_scale: [0.1]
    training.epochs_power_of_2: [17]
    training.n_message_steps: [5]
    training.learning_rate: [4e-4]
    training.meta_batch_size: [256]
    damage.target_damage_fraction: [0.2]
    pool.expected_updates: [256]
    damage.enabled: [true]
