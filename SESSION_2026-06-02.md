# SODC session log — 2026-06-02 (frugal)

Goal: crisp up the SODC live demo (jitter + defaults on random shuffle+damage),
then stand up cluster training to re-derive a clean 12×12 demo model.
Detailed records linked; this is the high-signal index.

## Diagnosis  (details → `web_demo/DEPLOY_OPTIMIZATIONS.md`, wiki deposit)
- **Jitter** (deployed 12-bit `1u5ssulx`) = shuffle-triggered **limit cycle**; a
  clean topology is an exact fixed point. **Defaults** = per-topology capability
  gap, not uniform → best-of-N topology → ~0.99. Logit sharpening is inert
  (hard bit = sign-crossing). Probe: `web_demo/export/ceiling_probe.py` (env `bool_nca`).
- 14×14 `dist_pe+rwse` (`6mo8q61y`): topo-avg 0.925; **3/24 topologies → 1.0000**;
  E[best-of-8] 0.991. Stable (tail-std ~1e-3).

## Cluster training — first hsm SSH-Slurm use → S3IT  (details → `HPC-Sweep-Manager/FIRST_USE_FEEDBACK_S3IT.md`)
- Env: conda **`bool_nca`** on cluster (`module load miniforge3/25.3.0-3`; `jax[cuda12]==0.10` + deps + pandas).
- hsm remote **`uzh`** (`backend: slurm`) in `.hsm/config.yaml`; workdir
  `/scratch/gbena/hsm-runs`, archive `/shares/payvand.ini.uzh/hsm-archive`,
  account `payvand.ini.uzh`, qos `medium`, gpu A100. Results stream to
  `/mnt/8TB_HDD/gbena/hsm-sweeps` (machine-wide `sweeps_root`).
- **Maintenance reservation 2026-06-03 06:00–18:00** → jobs must fit before 06:00.

## Bugs found & fixed (the saga that ate the session)
1. W&B auth missing on cluster → user ran `wandb login` on the cluster.
2. hsm conda-init didn't find S3IT's module-based conda → fell back to a
   **micromamba bridge** → wrong/CPU env. FIX (cluster-side):
   `ln -sfn /apps/u24/opt/x86_64_v3/miniforge3-25.3.0-3-parbelp6xzi5ketwmb6iavf6xur4z55o ~/miniforge3`.
3. **`train.py:41` hardcoded `CUDA_VISIBLE_DEVICES="3"`** → silent CPU on the
   cluster (allocated GPU is index 0). FIX: guard `if "SLURM_JOB_ID" not in env
   and "CUDA_VISIBLE_DEVICES" not in env`. **This was the "awfully long" cause**
   — 6.9 s/it (CPU) → ~15 it/s (GPU), ~100×.
4. Missing dep `pandas` (added to cluster env + `pyproject.toml`).
5. hsm bugs (logged in the feedback doc): `$USER` not expanded in the rsync
   workdir; dry-run doesn't render the per-remote spec; `--remote`+`--mode array`
   doc/CLI mismatch; **hsm reports FAILED jobs as COMPLETED** (verified via sacct);
   the conda-init bridge (#2); `module: command not found` in non-login sbatch.
- Memory metric `system/gpu_peak_gb` added to `train_loop.py` (true peak via
  `jax.devices()[0].memory_stats()`; train.py already sets PREALLOCATE=false).

## Sweep (running)  →  `sweeps/sweep_demo_12.yaml`
- 8 runs, 12×12 reverse, T=5, data_per_batch=256, mb=128, seed=0:
  1–4 PE ablation {layer_PE | dist_pe | rwse | dist_pe+rwse} on random+damage;
  5 random-clean; 6 max_neighbors=33; 7–8 fixed coherence (clean/damage).
  random=2¹⁸, fixed=2¹⁶+stop_accuracy.
- Launched **`sweep_20260602_172806`** (jobs 3692013–20), group `demo_sweep_12`,
  walltime 9h. ~7h/run on A100 (≈10 GB). Beta validated end-to-end first.
- Monitoring: self-paced `/loop` (task #6); completion via the launch bg process.

## Pending  (tasks #3,4,7,8,9)
- Post-sweep: pick winning PE + checkpoint; **probe + curate topologies** (#7);
  **port dist_pe+rwse to TS only if the PE ablation shows it beats layer_PE** (#8);
  apply hsm fixes (#9); optional long-horizon BPTT side-probe (#4).
