---
alwaysApply: true
---
**IMPORTANT**: Before executing any terminal command that runs Python or project tools, use the project's virtual environment. From the repo root: `source .venv/bin/activate` (created by `uv sync` per `docs/environment_setup.md`). Do not activate the obsolete conda environment `metabool`.
